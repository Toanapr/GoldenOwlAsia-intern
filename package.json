{
  "name": "g-scores",
  "version": "0.1.0",
  "private": true,
  "description": "Vietnam national high school exam score dashboard",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "migration:run": "npm run migration:run -w backend",
    "migration:run:prod": "npm run migration:run:prod -w backend",
    "migration:revert": "npm run migration:revert -w backend",
    "data:import": "npm run data:import -w backend --",
    "data:import:prod": "npm run data:import:prod -w backend --",
    "dev": "concurrently --kill-others-on-fail --names backend,frontend --prefix-colors blue,magenta \"npm run start:dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w frontend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "format": "prettier --write \"{backend,frontend}/**/*.{ts,tsx,js,mjs,json,css,md}\" \"*.{json,md}\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2"
  }
}
